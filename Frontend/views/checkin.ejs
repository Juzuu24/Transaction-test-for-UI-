<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Daily Check-in</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    body {
      background: white;
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      margin: 0;
      padding: 40px 20px;
      color: #333;
    }
    .container {
      max-width: 800px;
      margin: 60px auto;
      background: white;
      padding: 40px;
      border-radius: 15px;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
      text-align: center;
    }
    .header {
      text-align: center;
      margin-bottom: 30px;
      color: #2c3f51;
    }
    .header h1 {
      font-size: 2rem;
      margin: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 0.7rem;
      color: #2c3f51;
    }
    .brand {
      font-size: 2rem;
      color: #2c3f51;
      font-weight: bold;
      margin-bottom: 10px;
    }
    .info {
      background: #f3f3f3;
      padding: 20px;
      border-radius: 10px;
      border-left: 5px solid #2c3f51;
      margin-bottom: 25px;
      color: #555;
      font-size: 16px;
    }
    .reward-box {
      background: #e9e4f0;
      color: #4a148c;
      padding: 12px;
      border-radius: 8px;
      margin-top: 10px;
      font-weight: bold;
      border-left: 5px solid #2c3f51;
      display: inline-block;
    }
    .checkin-btn {
      padding: 15px 30px;
      font-size: 16px;
      color: #fff;
      background: linear-gradient(145deg, #2c3f51, #2c3f51);
      border: none;
      border-radius: 50px;
      cursor: pointer;
      box-shadow: 0 5px 15px rgba(106, 48, 147, 0.15);
      transition: all 0.3s ease-in-out;
      margin-top: 20px;
    }
    .checkin-btn:hover {
      background: linear-gradient(145deg, #a044ff, #2c3f51);
      box-shadow: 0 8px 20px rgba(106, 48, 147, 0.2);
    }
    .message {
      margin-top: 25px;
      font-weight: bold;
      font-size: 16px;
      color: #4a148c;
    }
    .logo {
      width: 60px;
      margin-bottom: 10px;
    }
    .search-bar {
      display: none;
    }
    .notification-icon {
      display: none;
    }
    @media (max-width: 768px) {
      body {
        padding: 20px 10px;
      }
      .container {
        padding: 30px 20px;
      }
      .header h1 {
        font-size: 2rem;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <img src="/source/logo.png" alt="Logo" class="logo">
      <div class="brand">CHAINX</div>
      <h1><i class="fas fa-calendar-check"></i> Daily Check-in</h1>
    </div>
    <div class="info">
      <p>Welcome back! Check in every day to collect your rewards and increase your streak.</p>
      <div class="reward-box">
        ✅ Today's Reward: <span style="color:#4a148c">50 points</span>
      </div>
    </div>
    <button class="checkin-btn" onclick="checkIn()">Check In Now</button>
    <div class="message" id="message"></div>
  </div>
  <script>
    function checkIn() {
      const messageDiv = document.getElementById("message");
      const today = new Date().toLocaleDateString();
      if (localStorage.getItem("lastCheckIn") === today) {
        messageDiv.textContent = "You have already checked in today! Come back tomorrow.";
        messageDiv.style.color = "#d9534f";
      } else {
        localStorage.setItem("lastCheckIn", today);
        messageDiv.textContent = "Check-in successful! You earned 50 points.";
        messageDiv.style.color = "#4a148c";
      }
    }
   </script>
</body>
</html>