<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Withdraw</title>
  <link rel="stylesheet" href="/style.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    
    .container {
      max-width: 800px;
      margin: auto;
      background: white;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    } 
    h2 {
      text-align: center;
    }
    label {
      display: block;
      margin-top: 15px;
    }
    .withdraw-info input[type="text"], input[type="number"] {
      width: 100%;
      padding: 10px;
      margin-top: 5px;
      border: 1px solid #ccc;
      border-radius: 5px;
    }
    button {
      margin-top: 20px;
      width: 100%;
      padding: 12px;
      background-color:#1F3A54 ; 
      ;
      color: white;
      border: none;
      border-radius: 5px;
      font-size: 16px;
      cursor: pointer;
    }
    button:hover {
      background-color:#2c3f51 ;
    }
    .info {
      background-color: #e9ecef;
      padding: 15px;
      border-radius: 5px;
      margin-bottom: 20px;
    }
  </style>
</head>
<body>
    <header class="header">
        <div class="logo"><img src="/source/logo.png" alt="Logo" class="logo"></div>
        <div class="brand">CHAINX</div>
        <div class="search-bar">
          <i class="fas fa-bars"></i>
          <input type="text" placeholder="Search">
          <i class="fas fa-search"></i>
        </div>
        <i class="fas fa-bell notification-icon"></i>
      </header>
  <div class="container">
    <h2>Withdraw Funds</h2>

    <div class="info">
      <p><strong>Current Balance:</strong> $<%= balance %> </p>
      <p><strong>Minimum Withdrawal:</strong> $100</p>
    </div>

    <form class="withdraw-info" method="POST" action="/withdraw">

      <label for="amount">Withdrawal Amount ($):</label>
      <input type="number" id="amount" name="amount" min="100" required />

      <label for="method">Withdrawal Method:</label>
      <input type="text" id="method" name="method" placeholder="e.g. Bank Transfer, PayPal" required />

      <label for="account">Account Details:</label>
      <input type="text" id="account" name="account" placeholder="Enter account number or email" required />

      <button type="submit">Submit Withdrawal</button>
    </form>
  </div>

  
  
</body>
</html>
