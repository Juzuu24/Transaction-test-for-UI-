<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('component/head') %>
  <title>Order Settings</title>
</head>
<body>

  <!-- Navbar -->
  <%- include('component/nav') %>

  <!-- Mobile Navbar -->
  <div class="mobile-navbar">
    <span class="menu-toggle" onclick="toggleSidebar()">☰ MENU</span>
  </div>

  <!-- Sidebar -->
  <%- include('component/sidebar') %>

  <!-- Main Content -->
  <div class="content">
    <div class="container-fluid">
      <div class="page-header">
        <h2>Order Settings</h2>
        <a href="/order/add" class="btn btn-primary">➕ Add New Setting</a>
      </div>
      
      <div class="table-container">
        <div class="table-responsive">
          <table class="table table-hover">
            <thead>
              <tr>
                <th>ID</th>
                <th>User</th>
                <th>Frequency</th>
                <th>Daily Limit</th>
                <th>Actions</th>
              </tr>
            </thead>
            <tbody>
              <% if (settings && settings.length > 0) { %>
                <% settings.forEach(setting => { %>
                  <tr>
                    <td><%= setting.id %></td>
                    <td><%= setting.username %></td>
                    <td><%= setting.lucky_frequency %></td>
                    <td><%= setting.lucky_daily_limit %></td>
                    <td>
                      <div class="btn-group">
                        <a href="/order/edit/<%= setting.id %>" class="btn btn-sm btn-warning">✏️ Edit</a>
                        <form method="POST" action="/order/delete/<%= setting.id %>" style="display:inline;">
                          <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Are you sure you want to delete this setting?')">
                            Delete
                          </button>
                        </form>
                      </div>
                    </td>
                  </tr>
                <% }) %>
              <% } else { %>
                <tr>
                  <td colspan="5" class="text-center py-4">No settings found</td>
                </tr>
              <% } %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>

  <script>
    // Sidebar Toggle Function
    function toggleSidebar() {
      var sidebar = document.getElementById("sidebar");
      sidebar.classList.toggle("active");
    }

    
    
  </script>
</body>
</html>